---
- name: Deploy WordPress for specific domain
  hosts: localhost
  connection: local
  gather_facts: false

  vars_files:
    - "../secrets/secrets.yml"
    - "../hostvars/{{ target_domain }}.yml"

  roles:
    - wordpress
