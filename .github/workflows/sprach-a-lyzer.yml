name: Sprach-A-Lyzer

on:
  push:
    paths:
      - "apps/sprach-a-lyzer/**"
      - "ansible/hostvars/templates/sprach-a-lyzer-hostvars.j2"
      - "ansible/playbooks/deploy-sprach-a-lyzer.yml"
      - "ansible/playbooks/roles/sprach-a-lyzer/**"
      - "scripts/sal-*.sh"
      - "scripts/verify-sal-deployment.sh"
      - ".github/workflows/sprach-a-lyzer.yml"
  pull_request:
    paths:
      - "apps/sprach-a-lyzer/**"
      - "ansible/hostvars/templates/sprach-a-lyzer-hostvars.j2"
      - "ansible/playbooks/deploy-sprach-a-lyzer.yml"
      - "ansible/playbooks/roles/sprach-a-lyzer/**"
      - "scripts/sal-*.sh"
      - "scripts/verify-sal-deployment.sh"
      - ".github/workflows/sprach-a-lyzer.yml"

permissions:
  contents: read

jobs:
  test:
    name: Quality and Golden Gate
    runs-on: ubuntu-latest
    timeout-minutes: 10
    services:
      postgres:
        image: postgres:17-alpine
        env:
          POSTGRES_DB: sprachalyzer_test
          POSTGRES_USER: sprachalyzer
          POSTGRES_PASSWORD: test-only
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U sprachalyzer -d sprachalyzer_test"
          --health-interval 2s
          --health-timeout 3s
          --health-retries 20
    env:
      SAL_TEST_DATABASE_URL: postgres://sprachalyzer:test-only@localhost:5432/sprachalyzer_test?sslmode=disable
    defaults:
      run:
        working-directory: apps/sprach-a-lyzer
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-go@v7
        with:
          go-version-file: apps/sprach-a-lyzer/go.mod
          cache-dependency-path: apps/sprach-a-lyzer/go.sum
      - name: Format check
        run: test -z "$(gofmt -l .)"
      - name: Vet
        run: go vet ./...
      - name: Protected deployment contract
        run: bash ../../scripts/verify-sal-deployment.sh
      - name: Production image build
        run: docker build --pull --tag sprach-a-lyzer:deployment-test .
      - name: v0.6 closure release gate
        run: bash ./scripts/verify-v0.6-closure.sh
      - name: Test with race detector
        run: go test -race ./...
